export class MyDate {
    year: number;
    month: number;
    day: number;

    constructor(year:number, month: number, day: number){
        this.year = year;
        this.month = month;
        this.day = day;
    }
}

//en la consola podemos correr el archivo 'ts' con => npx ts-node src/02-methods.tsT